<% if user[:avatar].attached? %>
  <%= cl_image_tag user[:avatar].key, class: "avatar dropdown-toggle"%>
<% else %>
  <%= image_tag "user.png", alt: "User", class: "avatar dropdown-toggle"%>
<% end %>